# Day01 - 初识 Python

- 学习状态：进行中
- 学习日期：2026-07-19
- 课程讲义：[Day01 - 初识 Python](../../Day01-20/01.%E5%88%9D%E8%AF%86Python.md)

## 今日目标

- 了解 Python 的主要特点、优点和应用领域。
- 在 macOS 上确认 Python 开发环境。
- 学习 Python 最基础的交互式操作和语法。
- 明确后续使用 VS Code 和 `.py` 文件的学习方式。

## 环境检查

- [x] 完成 Python 环境安装
- [x] 确认 Python 版本：Python 3.14.3
- [x] 确认 pip 可用：pip 25.3（Python 3.14）
- [x] 确认终端可以运行 Python
- [ ] 确认代码编辑器可用
- [x] 运行一个最简单的 Python 命令

## 今天学习的内容

- 使用 `python3` 启动 Python 交互解释器，使用 `exit()` 或 `Control + D` 退出。
- 使用 `print()` 输出字符串和计算结果。
- 区分字符串 `str` 和整数 `int`。
- 理解引号中的内容是字符串，没有引号的数字可以参与数学运算。
- 理解 `+` 对整数表示加法、对字符串表示拼接。
- 理解“字符串 × 整数”表示重复字符串，整数写在左边或右边都可以。
- 创建变量、给变量赋值，并理解重新赋值会更新变量保存的值。
- 学习变量命名规则：不能包含空格、不能以数字开头，可以使用下划线。
- 使用 `input()` 接收用户输入，并确认其返回值默认是 `str`。
- 理解交互解释器适合验证简短语句，完整程序应写入 `.py` 文件。

## 核心知识

以下课程理论问题尚未正式作答，留到下次完成：

1. Python 是什么？
2. Python 适合解决哪些问题？
3. Python 的主要优点和不足是什么？

## 练习与掌握情况

- 成功输出 `hello, python` 和整数加法结果。
- 能区分 `"15"` 与 `15` 的数据类型。
- 能判断字符串拼接、整数加法和字符串重复的结果。
- 能创建、输出和重新赋值变量。
- 能使用 `input()` 获取姓名和年龄，并通过 `type()` 确认两者都是 `str`。
- 已完成 4 道复盘题，理解变量、类型不匹配和 `input()` 返回类型。

## 遇到的问题

| 问题或报错 | 原因 | 解决方法 |
|---|---|---|
| `python3 -v` 输出大量导入信息并进入交互模式 | 小写 `-v` 表示 verbose，不是查看版本 | 使用大写 `python3 -V` 或 `python3 --version` |
| `pip3 -version` 报 `no such option: -e` | 长选项需要两个连字符 | 使用 `pip3 --version` |
| `print(''hello, python''` 报 `SyntaxError` | 重复使用单引号，并缺少右括号 | 使用一对匹配的引号并补全括号：`print('hello, python')` |
| `"15" + 5` 无法运算 | `str` 和 `int` 不能直接使用 `+` | 根据目的统一类型：字符串拼接或整数加法 |
| 误以为 `"3" * 4` 和 `2 * "Go"` 会报错 | 不熟悉 Python 的字符串重复规则 | 记住字符串与整数相乘会重复字符串，顺序不影响结果 |
| 变量名 `your daddy` 报 `SyntaxError` | 变量名不能包含空格 | 使用下划线，例如 `your_daddy` |
| 两条赋值语句写在同一行报 `SyntaxError` | 两条语句之间没有换行或分隔符 | 在交互解释器中逐行输入；完整程序放入 `.py` 文件 |
| 直接输入 `jie` 报 `NameError` | Python 将其当作未定义的变量名 | 输出变量 `name`，或把文字写成字符串 `"jie"` |

## 今日总结

- 我今天学会了：验证 Python 和 pip 环境，使用交互解释器、`print()`、变量和 `input()`，并初步理解 `str` 与 `int`。
- 我容易出错的地方：命令参数的大小写和连字符、引号与括号配对、变量命名、字符串和整数的类型区别。
- 我还需要完成：VS Code 与 `.py` 文件实践、课程理论问题和 Day01 综合练习。
- Day01 当前保持“进行中”，暂不标记完成。

## 下一步

- [ ] 在 VS Code 中打开 `my-learning/Day01/`。
- [ ] 创建并运行第一个 `.py` 文件。
- [ ] 回答 Python 简介、应用、优点和不足三个理论问题。
- [ ] 学习基础类型转换：`int()`、`float()`、`str()`。
- [ ] 完成使用变量、`input()`、`print()` 和类型转换的综合练习。
- [ ] 完成最终复盘后，由本人明确确认“今天学习完成”。
